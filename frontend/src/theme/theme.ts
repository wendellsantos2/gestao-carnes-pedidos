import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#b71c1c',
    },
    secondary: {
      main: '#5d4037',
    },
    background: {
      default: '#fafafa',
    },
  },
})

export default theme
